---
- name: Deploy HashiCorp Vault in Dev Mode
  hosts: master_nodes
  gather_facts: yes
  vars_files:
    - "{{ ROOT_DIR }}/vars/main.yml"
  
  roles:
    - role: deploy_dev
      tags: ['vault', 'deploy']
